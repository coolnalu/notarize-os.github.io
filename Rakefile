def execute(command)
  system "#{command}"
end

desc 'Jekyll Serve'
task :serve do
  execute("JEKYLL_GITHUB_TOKEN=0b8783534f207d89aafcc2be86f1bca889a466f1 jekyll serve --config _config.yml,_config_dev.yml")
end

